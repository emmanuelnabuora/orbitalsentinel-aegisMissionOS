# packages/

Shared code consumed by apps.

- `tsconfig/` — base TypeScript config (strict) extended by every TS package.
- `ui/` (Phase 2) — OrbitalSentinel component library built on shadcn/ui.
- `api-client/` (Phase 2) — typed client generated from the OpenAPI spec.
