# ai/

SentinelAI (LangGraph agents, prompt library, evaluation harness) and the
Qdrant collection schemas live here. Lands in Phase 3, after the core domain
model exists — the intelligence layer reasons over missions, assets, and
alerts, so those entities must be real first.
